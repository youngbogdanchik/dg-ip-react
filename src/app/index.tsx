import React from 'react';
import { Container } from '@mui/material';
import Routing from '../pages';

const App = () => {
  return (
    <Container>
      <Routing />
    </Container>
  );
};

export default App;
