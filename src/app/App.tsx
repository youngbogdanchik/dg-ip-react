import React from 'react';
import { Container } from '../shared/ui/kit/mui';
import Routing from './routing';

const App = () => {
  return (
    <Container>
      <Routing />
    </Container>
  );
};

export default App;
