import React from 'react';
import { Container } from 'shared/ui/kit/mui';
import { Router } from 'pages/router';

const App = () => {
  return (
    <Container>
      <Router />
    </Container>
  );
};

export default App;
