import React from 'react';
import { Container } from 'shared/ui/kit/mui';
import { Routing } from 'pages';

const App = () => {
  return (
    <Container>
      <Routing />
    </Container>
  );
};

export default App;
